import { Typography, styled } from '@mui/material';

export default styled(Typography)({
  color: 'var(--Black, #0D0D0D)',

  fontSize: '20px',
  fontStyle: 'normal',
  fontWeight: 400,
  lineHeight: '150%', // or "30px" if needed
});
