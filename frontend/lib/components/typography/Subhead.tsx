import { Typography, styled } from '@mui/material';

export default styled(Typography)({
  color: 'var(--Black, #0D0D0D)',

  fontSize: '36px',
  fontStyle: 'normal',
  fontWeight: 500,
  lineHeight: '130%', // or "46.8px" depending on your design needs
});
